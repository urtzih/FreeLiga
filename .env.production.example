# Production Environment Variables (Railway/Vercel)
# Copy to .env in Railway deployment (Docker) and set via dashboard

# === DATABASE ===
# Example for Railway MySQL service: DATABASE_URL="${{MYSQLDATABASEURL}}"
DATABASE_URL=

# === SECURITY ===
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

# === SERVER ===
NODE_ENV=production
PORT=3001
HOST=0.0.0.0

# === FRONTEND ===
# Public URL of Vercel deploy
FRONTEND_URL=
ALLOWED_ORIGINS=

# === FRONTEND BUILD VARS ===
VITE_API_URL=
VITE_API_TIMEOUT=30000
