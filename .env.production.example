# FreeSquash League - Production Environment Variables

# ⚠️ IMPORTANTE: NO subir este archivo a repositorios públicos
# Copiar a .env en el servidor de producción

# === BASE DE DATOS ===
# Para Alwaysdata (credenciales específicas)
DATABASE_URL="mysql://442318:freesquash_liga_pass@mysql-freesquash.alwaysdata.net:3306/442318_freesquash_liga"

# Para Railway (se genera automáticamente)
# DATABASE_URL="${{MYSQL.DATABASE_URL}}"

# === SEGURIDAD ===
# Genera un secreto fuerte con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="CAMBIAR_ESTO_POR_UN_SECRETO_ALEATORIO_Y_SEGURO"

# === SERVIDOR ===
NODE_ENV="production"
PORT=3001
HOST="0.0.0.0"

# === FRONTEND ===
# URL donde estará desplegado el frontend
FRONTEND_URL="https://freesquash.alwaysdata.net"
# Para Railway: FRONTEND_URL="${{RAILWAY_PUBLIC_DOMAIN}}"

# === CORS ===
# Orígenes permitidos (separados por coma si hay más de uno)
ALLOWED_ORIGINS="https://freesquash.alwaysdata.net"
