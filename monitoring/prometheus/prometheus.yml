global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Prometheus self-monitoring
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Node exporter (si se añade)
  - job_name: 'node'
    static_configs:
      - targets: ['node-exporter:9100']
